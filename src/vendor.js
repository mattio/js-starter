// Vendor libs. Used by Webpack in the production build only.
// Just one form of code splitting. Code here won't be in the main application bundle, so it can change frequently without redownloading.
/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
